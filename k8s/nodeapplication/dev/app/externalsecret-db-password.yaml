apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: nodeapp-app-db-password
  namespace: storefront-devhm
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: deepagent-aws
    kind: ClusterSecretStore
  target:
    name: app-env
    creationPolicy: Merge
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: teams-dev/storefront-devhm/DB_PASSWORD
