apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: nodeapp-app-postgres-password
  namespace: storefront-devno
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: deepagent-aws
    kind: ClusterSecretStore
  target:
    name: app-env
    creationPolicy: Merge
  data:
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: storefront-dev/storefront-devno/POSTGRES_PASSWORD
